/* Повторять цикл, пока ввод неверен */
'use strict';

/* Повторять цикл, пока ввод неверен */
'use strict';

let value = 0;

while (value <= 100 && value !== null) {
  value = prompt('Введите число больше 100', '');
}
